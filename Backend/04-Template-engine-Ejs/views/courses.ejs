<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Courses</title>
  </head>
  <body>
    <h2>You are registered in the following courses</h2>

    <ul>
      <% for(let i = 0; i < courses.length ; i++){ %>
      <li><%= courses[i] %></li>
      <% } %>
    </ul>

    <ul>
      <% for(let course of courses){ %>
      <li><%= course %></li>
      <% } %>
    </ul>
  </body>
</html>
